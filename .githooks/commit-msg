#!/bin/sh
# Strip Cursor co-author trailers so commits show only the human author on GitHub.
MSG_FILE="$1"
[ -f "$MSG_FILE" ] || exit 0

tmp="${MSG_FILE}.strip"
grep -v -E '^Co-authored-by:[[:space:]]*Cursor[[:space:]]*<' "$MSG_FILE" > "$tmp" || exit 0
mv "$tmp" "$MSG_FILE"
exit 0
